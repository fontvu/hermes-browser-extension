{
  "schema_version": 1,
  "contracts": {
    "profiles_list": { "fields": ["id", "name", "model", "label"], "entry_count_cap": 200 },
    "canonical_session": { "keys": ["session_id", "profile", "canonical_id"], "note": "additive note field only" },
    "group_projection": {
      "version": 3,
      "caps": ["read", "hide", "rename", "delete"]
    },
    "message_agent": { "available": true },
    "routine_cron": { "available": true }
  }
}
